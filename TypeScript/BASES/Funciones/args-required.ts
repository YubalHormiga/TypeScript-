(() => {
    const fullName = (firtsName: string, lastName: string ): string => {

        return `${firtsName} ${lastName}`

    }
    const name = fullName('Yubal', 'Hormiga')
    console.log(name)
})()